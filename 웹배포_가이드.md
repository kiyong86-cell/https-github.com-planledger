# 웹 서비스로 공개하기 — 단계별 가이드

이 앱은 두 가지 모드로 동작합니다:

- **로컬 모드** (지금): `시작하기.bat` 실행, 이 컴퓨터의 `data/` 폴더에 저장. 설정 없이 그대로 유지됩니다.
- **클라우드 모드**: Supabase 설정값을 넣으면 자동 전환 — 로그인 화면이 생기고, 사용자마다 데이터가 분리되어 클라우드에 저장됩니다.

아래 3단계를 순서대로 진행하면 웹에 공개됩니다. 모두 무료로 시작할 수 있습니다.

---

## 1단계. Supabase 준비 (약 10분)

1. https://supabase.com 접속 → 가입(GitHub 계정으로 가입 가능) → **New Project** 생성
   - Region은 **Northeast Asia (Seoul)** 선택
2. 왼쪽 메뉴 **SQL Editor** → `supabase/schema.sql` 파일 내용 전체를 붙여넣고 **Run**
   - 기획안·영수증 테이블, 파일 저장소, "본인 데이터만 접근" 보안 규칙이 자동 생성됩니다
3. **Settings → API**에서 두 값을 복사:
   - `Project URL`
   - `anon public` 키
4. (선택) 이메일 인증 없이 바로 가입되게 하려면: **Authentication → Providers → Email → Confirm email 끄기**
5. (선택) 구글/카카오 로그인: **Authentication → Providers**에서 Google, Kakao를 켜고,
   각 개발자 콘솔(Google Cloud Console / Kakao Developers)에서 발급받은 클라이언트 ID·Secret 입력.
   이 부분은 README의 소셜 로그인 섹션 참고. 안 켜도 이메일 로그인은 동작합니다.

**내 컴퓨터에서 클라우드 모드 테스트**: 프로젝트 폴더에 `.env.local` 파일을 만들어
`.env.example`처럼 두 값을 채우고 `시작하기.bat` 재실행 → 로그인 화면이 나오면 성공.
(`.env.local`을 지우면 다시 로컬 모드로 돌아옵니다)

## 2단계. GitHub에 코드 올리기 (약 10분)

배포 서비스(Vercel)는 GitHub에서 코드를 가져갑니다.

1. https://github.com 가입 → **New repository** (이름 예: `bizplan-app`, **Private** 선택)
2. 이 폴더에서 Git Bash 또는 PowerShell을 열고:

```bash
git init
```

```bash
git add .
```

```bash
git commit -m "기획안·영수증 관리 웹앱"
```

```bash
git remote add origin https://github.com/깃허브아이디/bizplan-app.git
```

```bash
git push -u origin master
```

- `data/` 폴더(기용님의 실제 데이터)는 `.gitignore`에 의해 **올라가지 않습니다** — 안심하세요.

## 3단계. Vercel로 배포 (약 10분)

1. https://vercel.com 가입 (GitHub 계정으로)
2. **Add New → Project** → 방금 만든 저장소 선택 → **Import**
3. **Environment Variables**에 두 값 입력:
   - `NEXT_PUBLIC_SUPABASE_URL` = Supabase Project URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = anon public 키
4. **Deploy** 클릭 → 몇 분 뒤 `https://프로젝트명.vercel.app` 주소가 생깁니다
5. Supabase 대시보드 → **Authentication → URL Configuration**:
   - Site URL: `https://프로젝트명.vercel.app`
   - Redirect URLs에 `https://프로젝트명.vercel.app/auth/callback` 추가

이제 이 주소를 아는 사람 누구나 가입해서 쓸 수 있습니다.

---

## 이후 할 일 (참고)

- **도메인 연결**: 가비아 등에서 도메인 구입(연 1.5~2만원) → Vercel 프로젝트 Settings → Domains에서 연결
- **수익화 시작 시**:
  - Vercel 무료 플랜은 상업용 제한이 있어 유료 전환 시 Pro($20/월) 필요
  - 결제는 토스페이먼츠/포트원 연동 (사업자 필요 — 두레줄기 협동조합 명의 사용 가능한지 확인)
  - 이용약관·개인정보처리방침 페이지 필수
- **코드 수정 후 재배포**: 수정 → `git add . && git commit -m "설명" && git push` → Vercel이 자동으로 다시 배포

## 자주 묻는 것

- **기존에 입력한 내 데이터는?** 로컬 모드 데이터(`data/` 폴더)는 클라우드로 자동 이전되지 않습니다.
  웹 버전에 가입한 뒤 필요한 기획안만 다시 만들거나, 이전이 필요하면 Claude에게 "로컬 데이터를 클라우드로 옮겨줘"라고 요청하세요.
- **비용은?** Supabase 무료(DB 500MB·파일 1GB), Vercel 무료로 시작. 사용자가 수십 명 수준이면 무료 한도로 충분합니다.
